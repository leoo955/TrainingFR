{
  "version": 2,
  "buildCommand": "npx turbo build --filter=web --no-cache && cp -r apps/web/dist ./public",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "apps/api-bot/src/index.ts"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
