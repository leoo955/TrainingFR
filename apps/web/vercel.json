{
  "version": 2,
  "buildCommand": "cd ../.. && npx turbo build --filter=web --no-cache",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
